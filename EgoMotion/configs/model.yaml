model:
  latent_dim: 256
  backbone: resnet34
  temporal_layers: 4
  temporal_heads: 8
  dropout: 0.1
  pretrained: true
  freeze_backbone: false
  use_motion_branch: true
  aggregate_last_k: 4

decoder:
  hidden_dim: 512
  occ_shape: [7, 15, 15]
